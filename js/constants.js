const CONSTANTS={}

document.addEventListener('DOMContentLoaded', () =>{
    CONSTANTS.CV = document.getElementById('display')
    CONSTANTS.CTX = CONSTANTS.CV.getContext('2d')
})


export default CONSTANTS